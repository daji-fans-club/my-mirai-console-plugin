pipeline {
    agent any

    stages {

        stage('git') {
            echo 'stage git is running'
        }

        stage('build') {
            echo 'stage build is running'
        }

        stage('run') {
            echo 'stage run is running'
        }

    }
}